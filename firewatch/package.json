{
  "name": "firewatch",
  "type": "module",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.html",
    "prod": "rm -r ./out; tsc && bun build src/index.html --outdir ./out --sourcemap=linked --minify --production --footer \"// You're looking at the minified version of the page's code! The full source code is available at https://github.com/purple-ic/forrust_fire/tree/main/firewatch\""
  },
  "devDependencies": {},
  "peerDependencies": {
    "typescript": "^5"
  }
}